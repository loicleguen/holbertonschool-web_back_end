#!/usr/bin/env python3
"""Module that defines a function to multiply two numbers with type annotations."""


def make_multiplier(multiplier: float, number: float) -> float:
    """Returns the product of multiplier and number."""
    return multiplier * number
