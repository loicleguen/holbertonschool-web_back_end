#!/usr/bin/env python3
''' concatenating two strings with type annotations '''


def concat(str1: str, str2: str) -> str:
    ''' concatenates two strings and returns a string '''
    return str1 + str2
